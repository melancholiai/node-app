const express = requier('express');

module.exports.getCurrentLocation = () => {

    return [32.099819, 34.801985];
};

module.exports.calculateDistance = (pointA, pointB) => {
    // ToDo: find way to calculate and return distance in km.
};